# Ignore node_modules in both frontend and backend
/frontend/node_modules/
/backend/node_modules/

# Ignore .env files in both frontend and backend
/frontend/.env
/backend/.env

# Optional: Ignore any .env files at the root level, if applicable
.env

# Optional: Ignore macOS system files (if using macOS)
.DS_Store
